DATABASE_URL=postgresql://postgres:postgres@localhost:5432/smart_scheduler
LINE_CHANNEL_ACCESS_TOKEN=LINE_CHANNEL_ACCESS_TOKEN
LINE_CHANNEL_SECRET=LINE_CHANNEL_SECRET
# C.4: รหัสยืนยันแอดมินเมื่อผูก LINE OA (ตัวอย่างจาก spec)
LINE_ADMIN_VERIFY_CODE=229
# C.1: base URL สำหรับลิงก์เช็คอินใน QR/LINE (เช่น https://som.develyst.online)
PUBLIC_CHECKIN_BASE_URL=

# LINE Developers → Messaging API → Webhook URL:
#   https://<domain>/api/webhooks/line   (ต้อง public ก่อน JWT — ไม่ใช้ Bearer token)
JWT_SECRET=JWT_SECRET
TZ=Asia/Bangkok
PORT=4006

# Auth (B.7). SKIP_AUTH=true bypasses JWT in dev so the FE (not yet sending tokens)
# keeps working. Remove/false in production once the FE logs in.
SKIP_AUTH=true
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
JWT_TTL_SECONDS=43200

# UC-012 end-of-day auto-cut. The Windows Task Scheduler exe (scripts/end-of-day.ts)
# sends this as the x-internal-secret header to POST /internal/jobs/end-of-day.
# Unset → that endpoint is disabled (503). Set a long random value in production.
INTERNAL_JOB_SECRET=

# UC-016 income ceiling. Base URL of the backoffice/ops API for teacher rate + cap.
# Unset → the ceiling stays unset (calendar still works). Set X-Service-Token via
# SERVICE_TOKEN if the ops pricing endpoint is locked down.
OPS_API_URL=http://localhost:3002
# SERVICE_TOKEN=
